The position chip. 1·2·3 get gold/silver/bronze metal; the rest are neutral numbered tiles.

```jsx
<RankBadge rank={1} size="lg" />
<RankBadge rank={7} />
```

Props: `rank` (1-based) · `size` (`sm`/`md`/`lg`).
